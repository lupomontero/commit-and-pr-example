{
  "name": "travis-cron-test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "update": "date > date.out",
    "test": "jest --verbose --coverage"
  },
  "license": "MIT",
  "devDependencies": {
    "jest": "^24.8.0"
  }
}
